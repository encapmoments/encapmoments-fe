import { Colors, Spacing, Typography, Fontsizes } from "../../styles/stylesIndex";
import { StyleSheet } from "react-native";

const MissionModalStyles = (width, height) => StyleSheet.create({

});

export default MissionModalStyles;
