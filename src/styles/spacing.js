import { scale } from 'react-native-size-matters';

const Spacing = {
    xs: scale(4),
    sm: scale(8),
    input: scale(12),
    lg: scale(16),
    xl: scale(24),
    xxl: scale(32),
};

export default Spacing;
