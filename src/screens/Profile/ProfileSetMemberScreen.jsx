import { View, Text, Image } from 'react-native';
import ProfileSetMemberScreenStyles from './ProfileSetMemberScreenStyles';

const ProfileSetMember = () => {
    return (
        <View />
    );
};

export default ProfileSetMember;
