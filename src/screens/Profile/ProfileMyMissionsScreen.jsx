import { View, Text, Image } from 'react-native';
import ProfileSetMemberScreenStyles from './ProfileMyMissionsScreenStyles';

const ProfileMyMissionsScreen = () => {
    return (
        <View />
    );
};

export default ProfileMyMissionsScreen;
